import 'package:flutter/material.dart';

class BrandColors {
  /// Primary brand color (used as seed)
  static const Color primary = Color(0xFF062A5A);

  /// Secondary brand color (explicit override)
  static const Color secondary = Color(0xFFFFD230);
}
